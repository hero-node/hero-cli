{
  "name": "hero-template",
  "version": "0.0.1",
  "dependencies": {
    "axios": "^0.16.1",
    "hero-cli": "github:hero-mobile/hero-cli",
    "hero-js": "github:hero-mobile/hero-js",
    "promise": "^7.1.1"
  },
  "devDependencies": {
    "body-parser": "^1.13.3",
    "chalk": "1.1.3",
    "cookie-parser": "^1.4.0",
    "express": "^4.13.3",
    "http-proxy": "^1.16.2",
    "log4js": "^0.6.35",
    "mockjs": "^1.0.1-beta3"
  },
  "scripts": {
    "start": "hero start -e dev",
    "build": "NODE_ENV=production hero build -e prod",
    "build-without-sourcemap": "NODE_ENV=production hero build -e prod -m",
    "build-standalone": "NODE_ENV=production hero build -e prod -s",
    "mock": "NODE_ENV=development node mock/index.js"
  }
}
