{
  "name": "hero-template",
  "version": "0.0.1",
  "dependencies": {
    "axios": "^0.16.1",
    "hero-cli": "github:hero-mobile/hero-cli",
    "hero-js": "github:hero-mobile/hero-js",
    "promise": "^7.1.1",
    "qs": "^6.4.0"
  },
  "scripts": {
    "start": "hero start -e dev -f",
    "build": "hero build -e prod -m -f",
    "build-framework": "hero build -e prod -m -f -b",
    "build-basic": "hero build -e prod -m -f -s",
    "mock": "node mock/index.js"
  }
}
