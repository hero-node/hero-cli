{
  "name": "hero-template",
  "version": "0.0.1",
  "dependencies": {
    "axios": "^0.16.1",
    "hero-cli": "github:hero-mobile/hero-cli",
    "hero-js": "github:hero-mobile/hero-js",
    "promise": "^7.1.1",
    "qs": "^6.4.0"
  },
  "scripts": {
    "start": "hero start -e dev -p 3000 -c webpack.config.dev.js",
    "build": "hero build -e prod -m -f -c webpack.config.prod.js",
    "build-framework": "hero build -e prod -m -f -b -c webpack.config.prod.js",
    "build-basic": "hero build -e prod -m -f -s -i -c webpack.config.prod.js",
    "serve": "hero serve",
    "android": "hero platform build android -e prod",
    "ios": "hero platform build ios -e prod"
  }
}
